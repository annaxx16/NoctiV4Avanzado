DATABASE_URL=postgresql+psycopg://umbra:umbra_dev@localhost:5432/umbra
REDIS_URL=redis://localhost:6379/0
LOG_LEVEL=INFO
MODE=sim

# Token para los endpoints /admin/* (halt, resume, flatten).
# Si queda vacío, esos endpoints se rechazan (fail-closed). Envíalo en el
# header X-Admin-Token. Genera uno: python -c "import secrets;print(secrets.token_urlsafe(32))"
ADMIN_TOKEN=
